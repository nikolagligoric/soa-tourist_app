export interface LoginDto {
  username: string;
  password: string;
}

export interface RegistrationDto {
  firstName: string;
  lastName: string;
  userName: string;
  password: string;
  email: string;
  role: string;
}